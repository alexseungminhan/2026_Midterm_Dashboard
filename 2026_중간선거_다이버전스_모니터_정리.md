# 2026 미국 중간선거 대시보드

**기준** 2026-08-09 · **선거일까지** 86일 · **보드** 30개 레이스 (상원·하원·주지사 각 10)
**스키마** v3.3.0 · **테스트** 142개 · **파이썬** 약 8,300줄

---

## 1. 이 프로그램이 하는 일

**예측하지 않습니다. 세 채널을 나란히 보여줍니다.**

```
베팅 시장 (Polymarket)      "시장은 민주당 51.0%로 본다"
여론조사                    "조사는 민주당 55.5%로 본다"
매크로 지표 4개 모델         "경제는 공화, 정치자금은 민주 쪽을 가리킨다"
```

셋을 **하나의 숫자로 합치지 않습니다.** 어긋나는 지점이 정보이고, 평균을 내면 그게 사라지기 때문입니다.

### v2를 왜 버렸나

v2는 이 셋을 `p_alpha = p_consensus + λ·signal`로 접었습니다. **그 λ = 0.10은 수기값이었고 한 번도 적합된 적이 없습니다.** 적합에 쓸 과거 표본이 47건뿐이고 그중 상당수가 자체 매니페스트에 "APPROXIMATE reconstructions"라고 적힌 근사 재구성값이었습니다.

즉 **보드 한복판의 대표 숫자를 아무도 방어할 수 없었습니다.** 섞지 않으면 λ를 정당화할 필요가 없습니다.

부수 효과로 **네브래스카 문제가 저절로 풀렸습니다.** v2는 폴리마켓("민주당이 이기는가" 0.01)과 폴(오스본 vs 리케츠 0.795)을 평균 내 0.215를 만들었는데, 이는 **어느 채널도 믿지 않는 숫자**였고 그 레이스를 하드코딩으로 제외해야 했습니다.

---

## 2. 보드는 시장이 정합니다

`races.json`은 더 이상 레이스 목록이 아닙니다. 현직·등급을 붙여주는 참조 테이블로 강등됐습니다.

`board.py`가 폴리마켓 `midterms` 태그를 전부 긁고(실측 697개 이벤트), 제목이 정확히 `<주> Senate Election Winner` 형태인 것만 레이스로 인정합니다. **누적 거래액 순 상위 10곳**씩 보여줍니다.

### ⚠️ 하원에서 거래액은 경쟁도가 아닙니다

폴리마켓은 하원 443개 지역구를 전부 상장하고, 누적 거래액은 "얼마나 오래 상장돼 있었나"에 가깝습니다. 그래서 **하원 상위권은 CA-28(민주 98.7%)·CA-15(97.5%) 같은 안전 의석**이 차지합니다.

손으로 고른 경합 18곳은 **전부 하원 중앙값 $23.7K 아래**였습니다. 이건 설계상의 한계이고, 사용자가 수치를 보고 내린 결정입니다.

---

## 3. 베팅 확률 — 세 가지 교정

| 교정 | 왜 |
|---|---|
| **양쪽 다리를 읽고 정규화** | 폴리마켓은 D·R 가격을 따로 매기고 합이 1이 아님 (텍사스 D 0.495 / R 0.515) |
| **정당별 합산** | 알래스카 주지사는 정글 프라이머리라 한 시장에 20여 명. 선두 공화 후보의 0.335는 공화당 확률이 아님 |
| **무소속 질량 보고** | 민주·공화 어느 쪽도 아닌 확률을 `unmapped_mass`로 별도 집계 |

**화면은 두 정당 비율이 아니라 시장 가격을 그대로 씁니다.** 네브래스카가 이유를 보여줍니다:

```
리케츠 72.5¢ · 무소속 26.5¢ · 민주 0.25¢

두 정당 비율:  공화 99.7%   ← 무소속에 걸린 돈을 지운 값
시장 가격:     공화 73%     ← 우리가 쓰는 값
```

두 정당 비율을 쓰면 **네브래스카가 무소속이 없는 와이오밍(97.6%)보다 확실한 선거로 보입니다.** 순위가 뒤집힙니다.

---

## 4. 의석수 — 계산하지 않고 읽습니다

`chambers.py`는 집계하지 않습니다. 애초에 불가능합니다 — 보드에는 30곳뿐인데 하원은 435석이고, **안 보여주는 레이스가 정확히 결과를 가정해야 하는 레이스**입니다.

| 시장 | 거래액 | 쓰임 |
|---|---|---|
| Balance of Power | $9.5M | 상·하원 조합 결과 |
| Which party will win the House | $9.1M | 하원 다수당 확률 |
| Which party will win the Senate | $3.8M | 상원 다수당 확률 |

**의석수는 폴리마켓 자기 페이지와 같은 방식**으로 냅니다 — 각 레이스의 현재 선두 + 이번에 선거가 없는 의석. 상원 `민주 52 / 공화 48`이 그 값이고, 폴리마켓 표시와 일치하도록 검증했습니다.

거친 추정인 것은 맞습니다: **50.1%짜리 선두도 99%짜리와 똑같이 한 석**으로 셉니다.

---

## 5. 여론조사

```
가중치 = 최신성(반감기 14일) × √표본크기 × 매치업 신뢰도
P(민주 승리) = Φ(가중평균 마진 / 6.0)
```

| 매치업 신뢰도 | 어떤 조사 |
|---|---|
| 1.00 | 양당 후보 확정, 이름으로 물음 |
| 0.60 | "민주당 후보 vs 공화당 후보" 일반 대결 |
| 0.35 | 예비선거 전 가상 대결 |
| 0.15 | 사퇴·낙선 후보 포함 |

**예비선거 여론조사는 제외합니다.** 가중치를 낮추는 게 아니라 뺍니다 — "민주당 경선에서 누가 이기나"는 아무리 작게 눌러도 "민주당이 공화당을 이기나"의 증거가 되지 못합니다.

**한 조사 = 한 줄** 규칙도 있습니다. 한 조사가 여러 대결을 물으면 그 기관의 무게가 대결 수만큼 뻥튀기됩니다(Marquette 7월 조사 하나가 5줄이었습니다).

출처는 **NYT 트래커에서 수기 취합**입니다. 2026년 무료 API가 없습니다.

---

## 6. 매크로 지표 4개 모델

**모두 오늘 값끼리 비교합니다. 과거 실행 기록을 이어받지 않습니다** (2026-08-09 개편).

| 모델 | 무엇을 | 어떻게 | 가동 |
|---|---|---|---|
| **경제** | 주 실업수당 청구·경기동행지수·실업률 | 각 지표의 **1년 전 대비 변화** | 30/30 |
| **정치자금** | 주내 기부 비중·소액기부 건수 (FEC) | 두 후보 **직접 비교** | 14/30 |
| **예비선거** | 2026 투표수가 2022년 대비 몇 % | 두 당 **직접 비교** | 14/30 |
| **관심도** | 위키 조회수·편집 횟수 | 두 후보 **직접 비교** | 16/30 |

### 경제의 귀속 규칙

경기가 좋아지면 **책임지는 쪽**에 유리합니다. 그 대상이 직위마다 다릅니다:

- **상·하원** → 대통령 정당 (현재 공화당)
- **주지사** → 그 자리를 쥔 정당

그래서 **같은 주인데 상원과 주지사의 부호가 반대**로 나옵니다.

### 합치는 방법 — 평균이 아니라 개수

세 경제 지표는 %·지수·%p로 **단위가 서로 달라 평균 낼 수 없습니다.** 어느 쪽을 가리키는지 **개수를 셉니다.** 0.1 미만 변화는 세지 않습니다 — 0.05% 변화가 한 표가 되면 안 되니까요.

표의 `매크로 지표` 칸도 같은 방식입니다. 네 모델의 다수결이고, 같은 수로 갈리면 `경합`입니다.

### 무엇을 잃었나

**수준을 재지 변화를 재지 않습니다.** 공화당이 원래 세 배씩 모금하던 지역구는 민주당이 올해 세 배로 늘려도 여전히 `공화 우세`로 읽힙니다. 의도적으로 감수한 거래입니다.

---

## 7. 이번 개편에서 잡은 실제 버그들

문서로 남길 가치가 있는 것들입니다. 전부 화면에 조용히 틀린 숫자를 띄우고 있었습니다.

| 버그 | 증상 | 원인 |
|---|---|---|
| **없는 값을 0으로** | 사우스캐롤라이나 1주일 거래액 `$0` | 폴리마켓이 거래 없는 이벤트에서 필드를 **생략**하는데 0으로 읽음 (697개 중 435개 해당) |
| **막대 길이/색 불일치** | 캔자스가 폭 18.8%짜리 **빨간** 막대 | 길이는 민주당 확률, 색은 앞서는 당 |
| **잔차화가 모델을 죽임** | 메인·텍사스·미시간·조지아 정치자금 **전부 결측** | 잔차는 음수가 되는데 새 대비 함수가 양수만 받음 |
| **동음이의어 문서** | 메인 위키 조회 민주 **99.8%** | 공화당 쪽이 `Susan Collins (disambiguation)`로 해결됨 (실제 문서는 주간 15,914회) |
| **무명 후보 하나가 레이스를 버림** | 40곳 중 28곳 관심도 결측 | 해결 못 한 후보 한 명 때문에 레이스 전체를 포기 |
| **후보 명단이 실제와 다름** | 메인이 `Graham Platner vs Patricia M. Collins` | FEC 등록 **전원**을 명단으로 씀. 시장의 실제 후보로 교체 |

**미시간을 예비선거 시트에 넣지 않은 이유**도 여기 속합니다. 2022년 민주당 주지사 예선에서 휘트머가 **무투표**였고, 2026년에는 공화당 로저스가 **무투표**였습니다. 비율을 내면 "민주당 열정 폭발"이라는 거대한 가짜 신호가 나옵니다 — 어느 당 예선에 경쟁자가 있었는지만 반영한 값으로요.

---

## 8. 반드시 함께 읽어야 할 한계

**① 눈금이 검증되지 않았습니다.** 세기 구간(0.25/0.75/1.50)과 최소 변화량(0.1)은 수기 설정값입니다. 그래서 **이 값들로 확률을 만들지 않고 방향만** 보여줍니다.

**② 하원 상위권은 접전지가 아닙니다.** 3절 참조. 누적 거래액 순이라 안전 의석이 올라옵니다.

**③ 관심도는 가장 약한 근거입니다.** 조회수는 **유명세이지 지지가 아닙니다.** 스캔들도 조회수를 올립니다.

**④ 정치자금은 절반만 채워집니다.** 주지사는 FEC 관할 밖이라 구조적으로 불가능합니다.

**⑤ 예비선거는 20개 주만 있습니다.** 수기 시트이고, 최근 세 번의 중간선거에서 **양당 모두 예선을 치른 주**만 담깁니다. 알래스카는 정글 프라이머리라 영원히 불가능합니다.

**⑥ 의석수는 거친 추정입니다.** 4절 참조.

---

## 9. 작동하지 않아서 삭제한 것

2026-08-09에 코드에서 **완전히 제거**했습니다. 가중치 0으로 남겨두면 커버리지 계산에 잡히고 `--skip` 같은 우회 옵션을 계속 요구합니다.

| 소스 | 왜 |
|---|---|
| GDELT | API 상시 429. 응답 본문이 스스로 "벌크 파일로 옮기라"고 안내 |
| 레딧 | OAuth 필수, 무인증 403 |
| 유튜브 | API 키 필요 |
| Kalshi | 티커 매핑이 비어 있어 한 번도 실행되지 않음 |
| 정당 등록 순변동 | 44개 주는 제도 자체가 없고, 있는 6개 주는 데이터 미입력 |
| 고유 기부자 수 | 소액기부 건수와 **r = +0.962** — 같은 것을 두 번 셈 |
| 자금 소진율 | −43.9 ~ 12,873으로 유계가 아님. 현금 부족을 활동으로 오독 |
| 재기부율 | 83개 레이스에서 값이 0.0/1.0 두 가지뿐 |

같이 없앤 것: `BaselineStore`(런 사이 상태), `backfill` 명령, `track_a/adapters.py`(폴리마켓은 board.py가, 폴은 manual.py가 직접 읽음), `make-templates`.

**이제 `python3 -m election2026 run` 한 줄이면 끝입니다.** `--skip`이 필요 없습니다.

---

## 10. 사용법

```bash
cd ~/Desktop/SCBK_Intern_Project/2026_election_prediction

python3 -m election2026 run              # 데이터 갱신 (20분 안팎)
python3 -m election2026.serve            # → http://localhost:8000/dashboard.html

python3 -m election2026 import-polls 2026_senate_poll.xlsx 2026_House_poll.xlsx Gubernatorial_Polls_2026.xlsx
python3 -m election2026 validate         # 스키마 검증
python3 -m pytest election2026/tests -q  # 142개 통과해야 정상
```

**여론조사 세 파일은 항상 같이** 넘기세요. 하나만 넘기면 나머지가 지워집니다.

`dashboard.html`은 **직접 열면 동작하지 않습니다** — `fetch`가 `file://`에서 막힙니다.

---

## 11. 배포 — 처음 한 번만 하는 설정

저장소: `github.com/alexseungminhan/2026_Midterm_Dashboard`

```
GitHub Actions (매일 20:00 UTC = 한국 05:00)
   → run → validate → forecast.json 커밋·푸시
      → Render Static Site 자동 재배포
```

**한 번 설정하면 이후로는 아무것도 하지 않아도 됩니다.** 여론조사 엑셀을 바꿀 때만 직접 커밋합니다.

### 1단계 — 커밋·푸시

```bash
git add -A && git status      # .env 가 목록에 없는지 확인
git commit -m "..." && git push
```

푸시가 `refusing to allow a Personal Access Token to create or update workflow`
로 거부되면, 토큰에 **`workflow` 권한**이 없는 것입니다.
https://github.com/settings/tokens → 해당 토큰 → `workflow` 체크 → `Update token`.
토큰 값은 그대로라 다시 로그인할 필요가 없습니다.

### 2단계 — FEC 키를 Secrets에 등록

```bash
grep FEC_API_KEY .env | cut -d= -f2 | tr -d '\n' | pbcopy   # 화면에 안 띄우고 복사
```

저장소 → **Settings → Secrets and variables → Actions → New repository secret**

- **Name**: `FEC_API_KEY` (오타 주의 — 대문자·언더바 그대로)
- **Secret**: 붙여넣기

한 번 저장하면 다시 볼 수 없습니다. 값이 필요하면 로컬 `.env`를 보면 됩니다.

### 3단계 — Actions에 쓰기 권한

**Settings → Actions → General → 맨 아래 `Workflow permissions`**
→ **`Read and write permissions`** 선택 → **`Save`**

안 하면 봇이 커밋은 만들어도 푸시에서 403으로 매일 실패합니다.

### 4단계 — 즉시 한 번 실행해 확인

새벽 5시까지 기다렸다가 실패를 발견하면 하루를 버립니다.

**Actions 탭 → `daily forecast` → `Run workflow`.** 약 20분.
성공하면 `data: 자동 갱신 YYYY-MM-DD` 커밋이 하나 생깁니다.

| 실패 지점 | 원인 |
|---|---|
| `Run pipeline` | 2단계 Secret 이름 오타 |
| `Commit if changed`에서 403 | 3단계를 안 했거나 `Save`를 안 누름 |
| `Validate` | 데이터 문제 — 로그를 확인 |

### 5단계 — Render 연결

**New + → Static Site** (Web Service 아님 — 상시 프로세스가 떠서 메모리를 먹습니다)

| 항목 | 값 |
|---|---|
| Branch | `main` |
| Root Directory | 비워둘 것 |
| Build Command | `bash build_public.sh` |
| Publish Directory | `public` |

빌드 로그에 `OK — public/ 준비 완료`가 뜨면 성공입니다.

### 6단계 — 맥의 로컬 자동 실행 끄기

```bash
launchctl bootout gui/$UID/com.scbk.election2026.daily
```

클라우드가 대신 도는데 맥에서도 돌면 **로컬 `forecast.json`이 매일 덮어써져
git 충돌**이 납니다. 같은 API를 두 번 두드리기도 합니다.

### 배포에서 실제로 막혔던 것 (2026-08-09)

`election2026/.gitignore`가 **`data/forecast.json`과 `data/history/*.json`을
무시**하고 있었습니다. 생성물이니 git에 넣지 않는다는 판단이었는데, 이 둘은
**배포 산출물**입니다. 그대로 뒀다면 둘 다 조용히 실패했을 것입니다:

* Render 빌드가 저장소에서 `forecast.json`을 못 찾아 실패
* Actions의 `git add`가 무시된 경로에는 아무것도 올리지 못해, 매일
  `"변화 없음 — 커밋 생략"`만 찍고 사이트는 첫 배포 상태로 정지

에러 없이 멈추는 종류라 며칠 뒤에야 알아챘을 것입니다.

### 호스팅 부하 (실측)

```
forecast.json  285.8 KB → gzip 13.3 KB
index.html      34.2 KB → gzip 12.8 KB
페이지 1회       320.1 KB → gzip 26.1 KB

방문 10만 회/월 → 2.49 GB   (Render 무료 100GB의 2.5%)
```

Static Site는 **서버 프로세스가 없어 메모리 0**입니다. 웹소켓·폴링도 없고,
`fetch`는 페이지 로드 시 한 번뿐이며 `cache: "no-cache"`라 재방문 시 파일이
안 바뀌었으면 304(본문 0바이트)를 받습니다.

`build_public.sh`가 배포 폴더를 화이트리스트로 조립하고 유출 검사(키·경로·
`.env` 값 대조)를 돌립니다. 걸리면 `exit 1`로 빌드를 중단합니다.

---

## 12. 남은 일

| 시점 | 할 일 |
|---|---|
| 8/11 이후 | 미네소타·위스콘신 예비선거 투표율 추가 (양당 경합 여부 확인 후) |
| 수시 | 여론조사 엑셀 갱신 — 자동화 불가 |
| 여유 생기면 | 위키 후보 문서 매핑 보강 (`config.TRACK_B['wiki_titles']`) |

**미시간·알래스카는 넣지 마세요.** 7절의 이유입니다.
