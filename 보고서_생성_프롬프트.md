# 결과 보고서 생성 프롬프트 (v3)

> **2026-08-08 v3 기준으로 다시 씀.** 이전 판은 v2의 FLAG·컨센서스·Δ·λ를
> 설명했는데 그 계층은 전부 삭제됐다. 이 문서를 쓸 것.
>
> **이전 판의 실패에서 배운 것:** 숫자를 프롬프트 본문에 박아 넣었더니 며칠
> 만에 낡았다. 이번엔 **숫자를 §A에서 기계로 뽑아 붙인다.** 서술·방법론·한계만
> 프롬프트가 들고 있는다.

---

## A. 먼저: 오늘 수치 뽑기

보고서를 만들기 전에 이걸 먼저 실행해서 출력을 복사해 둔다.

```bash
cd ~/Desktop/SCBK_Intern_Project/2026_election_prediction
python3 - <<'EOF'
import json
d = json.load(open("election2026/data/forecast.json"))
m = d["meta"]
print(f"기준 시각: {m['generated_at']}  ·  스키마 {m['schema_version']}")
print(f"보드: {len(d['races'])}개 레이스  ·  소스 {len(m.get('sources_used', []))}종")
print(f"결측 소스: {m.get('sources_missing')}")
print()
for b in d.get("balance_of_power", []):
    print(f"[BoP] {b.get('label')}: {b.get('prob')}")
print()
for name, ch in d["chambers"].items():
    print(f"[{name}] 다수당 확률 {ch.get('prob_dem_majority')} · "
          f"기대 의석 D {ch.get('expected_seats_dem')} / R {ch.get('expected_seats_rep')} "
          f"(근사={ch.get('expectation_is_approximate')})")
print()
print("race_id | chamber | 시장 | 폴 | 신뢰 | 거래액 | 미매핑")
for r in d["races"]:
    print(" | ".join(str(x) for x in [
        r.get("race_id"), r.get("chamber"), r.get("prob_dem_market"),
        r.get("prob_dem_polls"), r.get("trustworthy"),
        r.get("volume"), r.get("unmapped_mass")]))
print()
for r in d.get("movers", []):
    print(f"[mover] {r.get('race_id')}: {r.get('change')}")
EOF
```

키 이름이 안 맞으면 `python3 -m election2026 validate`로 현재 스키마를 확인하고
`election2026/schema.py`를 볼 것. **출력에 없는 숫자는 보고서에 쓰지 않는다.**

`forecast.json` 자체를 대화에 첨부할 수 있으면 그게 더 낫다.

---

> 아래 `---` 사이 전체를 복사해서 Claude에 붙여넣고, §A 출력을 같이 붙인다.

---

너는 데이터 저널리즘 수준의 시각적 리포트를 만드는 디자이너 겸 분석가야.
아래 내용으로 **단일 HTML 아티팩트** 형태의 종합 결과 보고서를 만들어줘.

## 0. 산출물 규격

- **형태**: 단일 HTML 페이지 (아티팩트). 외부 CDN·폰트·이미지 로드 없이 전부 인라인.
- **언어**: 한국어. 고유명사·수식 기호는 원문 유지.
- **길이**: 스크롤 기준 길어도 괜찮음. 내용을 줄이지 말고 **전부** 담아라.
- **대상 독자**: 정치 데이터 모델을 잘 모르는 회사 상급자 + 나중에 코드를 이어받을 개발자. 둘 다 읽을 수 있게 써라.
- **테마**: 라이트/다크 모두 대응 (`prefers-color-scheme` + `:root[data-theme]`).
- **반응형**: 표·차트는 각자 `overflow-x:auto` 컨테이너 안에서만 가로 스크롤. body는 절대 가로 스크롤 없게.
- **차트**: 순수 인라인 SVG 또는 CSS로 직접 그려라. 차트 라이브러리 금지.

## 1. 이 프로젝트가 무엇인가 (가장 중요 — 오해하지 말 것)

**예측 모델이 아니다. 세 채널을 나란히 보여주는 모니터다.** 보고서 전체가 이 구분 위에 서 있어야 한다.

일반 선거 예측 모델은 여러 증거를 하나의 확률로 접는다. 이 도구는 **접지 않는다.**

1. **베팅 시장**이 말하는 확률
2. **여론조사**가 말하는 확률
3. **기초 지표**가 말하는 방향과 세기

세 값은 서로 다르고, **다른 게 정상이고, 그 차이가 곧 정보다.** 하나로 평균 내면 그 차이가 사라진다.

### 이전 버전(v2)은 접었고, 그래서 접기를 그만뒀다 — 이 대목을 반드시 실어라

v2는 `p_alpha = p_consensus + λ·signal`로 셋을 하나로 만들었다. 그 **λ = 0.10은 수기값이었고 한 번도 적합된 적이 없다.** 적합에 쓸 과거 표본이 47건뿐이었고 그중 상당수가 자체 매니페스트에 "APPROXIMATE reconstructions"라고 적힌 근사 재구성값이었다. 즉 **보드 한복판의 대표 숫자를 아무도 방어할 수 없었다.**

v3는 섞지 않는다. 섞지 않으면 λ을 정당화할 필요가 없다. 그래서 `divergence.py`, `p_consensus`, `p_alpha`, `delta`, `flagged`, `backtest`, `consensus.py`가 전부 삭제됐다. **남은 것은 각 채널이 실제로 말한 값이다.**

부수 효과로 **네브래스카 문제가 저절로 풀렸다.** v2는 폴리마켓("민주당이 이기는가" 0.01)과 폴(오스본 vs 리케츠 0.795)을 평균 내 0.215를 만들었는데, 이는 **어느 채널도 믿지 않는 숫자**였고 그래서 그 레이스를 하드코딩으로 제외해야 했다. v3는 두 값을 그냥 나란히 놓고 "시장 확률의 26%가 민주·공화 어느 쪽도 아닌 후보에 걸려 있다"고 표시한다 — 하드코딩 없이 일반 규칙으로.

**교훈으로 한 문장:** 두 채널을 blend하려면 둘이 *같은 사건*을 가격 매기고 있어야 한다. 정의가 다르면 blend는 가짜 합의를 만든다.

### 기본 수치

§A 출력에서 그대로 가져와라. **거기 없는 숫자는 쓰지 마라.**

## 2. 아키텍처 — 세 채널

```
   ┌─ 베팅 시장 (Polymarket) ─────────────────────────────┐
   │  어떤 선거를 보여줄지도 이쪽이 정한다 —              │
   │  거래액 순으로 줄 세운 시장의 목록이 곧 보드다.      │
   │  의석수·다수당 확률도 챔버 시장에서 그대로 읽는다.   │
   └──────────────────────────────────────────────────────┘
   ┌─ 여론조사 ───────────────────────────────────────────┐
   │  수기 스프레드시트 (2026년 무료 API가 없다).         │
   │  마진 → 확률 Φ(margin / σ), σ = 6.0                  │
   └──────────────────────────────────────────────────────┘
   ┌─ 기초 지표 4개 모델 ─────────────────────────────────┐
   │  경제 / 정치자금 / 풀뿌리 / 관심도                    │
   │  "어느 쪽으로, 얼마나 세게" — 확률이 아니다.          │
   └──────────────────────────────────────────────────────┘

   세 채널은 끝까지 분리된 채 나란히 표시된다. 합성 단계가 없다.
```

기초 지표 트랙은 베팅·여론조사 데이터를 **읽지 않는다.** 여론조사 시트에서 후보 이름을 읽어 쓰는 것조차 금지돼 있다 — 그러면 "독립적인 증거"라는 전제가 깨지기 때문이다.

## 3. 보드는 누가 정하는가 (v3의 가장 큰 변화)

`data/reference/races.json`은 **더 이상 레이스 목록이 아니다.** 현직·등급·과거 결과를 붙여주는 참조 테이블로 강등됐다.

보드에 오르려면 **시장에서 거래되기만 하면 된다.** `board.py`가 폴리마켓 `midterms` 태그를 페이징으로 전부 긁고(2026-08-08 실측 640개 — 하원 443, 상원 35, 주지사 35 + 챔버 시장), 제목이 정확히 `<주> Senate Election Winner` / `<주> Governor Election Winner` / `<XX>-<NN> House Election Winner` 형태인 것만 레이스로 인정한다.

### ⚠️ 하원에서 거래액은 경쟁도가 아니다 — 반드시 실어라

폴리마켓은 하원 443개 지역구를 전부 상장한다. 누적 거래액은 "얼마나 오래 상장돼 있었나"에 가깝지 접전인지와 무관하다. 그래서 누적 상위권을 **CA-28·FL-01·MS-01 같은 안전 의석**이 차지한다.

실측: 사람이 손으로 고른 경합 18곳은 **전부 하원 중앙값 $23.7K 아래**였다 (최고 wa-03 $26,981, 최저 pa-08 $1,251, ca-22는 시장 자체가 없음).

그래서 `config.BOARD["rank_by"]` 기본값이 **24시간 거래액**이다 — "지금 실제로 사람들이 붙어 있는 시장"을 훨씬 잘 잡는다. 그래도 안전 의석이 상단에 올라올 수 있고 **그 행은 여론조사·경제 칸이 비어 보인다. 고장이 아니라 사실이다.**

**이건 설계상의 한계이고, 사용자가 수치를 보고 내린 결정이다.** 감추지 말고 그렇게 써라.

## 4. 베팅 확률 읽기 — 세 가지 교정 (차트 소재)

옛 방식(민주당 다리 가격을 그대로 읽기)은 세 군데서 틀렸다. `board._two_party()`가 전부 고친다.

1. **양쪽 다리를 읽고 정규화한다.** 폴리마켓은 D·R 가격을 따로 매기고 합이 1이 아니다 — 텍사스 상원 2026-08-08: **D 0.495 / R 0.515 (합 1.010)**. D만 읽으면 그 스프레드가 편향으로 들어온다. → `D / (D + R)`
2. **정당별로 합산한다.** 알래스카 주지사는 정글 프라이머리라 한 시장에 후보 20여 명이 올라간다. **선두 공화 후보의 0.335는 공화당 확률이 아니다.**
3. **민주·공화 어느 쪽도 아닌 확률을 보고한다** (`unmapped_mass`). **10%를 넘으면 `trustworthy = false`**가 되고 대시보드가 숫자 대신 경고를 띄운다. 현재 걸리는 곳: **네브래스카 26%, 몬태나 17%** (무소속 후보에 걸린 돈).

한쪽 정당 다리가 **아예 없으면 확률은 `None`이다. `0.0`이 아니다** — 다리가 없는 것과 "확률이 0"은 다른 사실이고, 후자로 표시하면 없는 확신을 만든다.

## 5. 의석수와 다수당 확률 — 계산하지 않고 읽는다

`chambers.py`는 집계하지 않는다. **애초에 불가능하다:** 보드에는 수십 개 레이스뿐인데 하원은 435석이고, 안 보여주는 레이스가 정확히 결과를 가정해야 하는 레이스다.

대신 시장을 직접 읽는다.

| 시장 | 거래액 | 쓰임 |
|---|---|---|
| Balance of Power: 2026 Midterms | $9.5M | 상·하원 조합 결과 (대시보드 최상단) |
| Which party will win the House | $9.1M | 하원 다수당 확률 |
| Which party will win the Senate | $3.8M | 상원 다수당 확률 |
| Republican Senate seats after… | $2.8M | 상원 의석 분포 |
| Republican House seats after… | $282K | 하원 의석 분포 |
| How many Republican Governors… | $686K | 주지사 분포 |

**다수당 확률은 의석 분포가 아니라 "어느 당이 이기는가" 시장에서 읽는다.** 장악은 의석 수의 깔끔한 함수가 아니기 때문이다 — 50-50 상원은 부통령이 가른다. 거래자들은 그 규칙을 반영해 가격을 매기지만, 중앙값 합산은 못 한다.

교차검증 하나: 상원 장악 시장이 민주 45.5%인데 의석 분포에서 P(공화 ≤ 49) = 46.2%로 거의 일치한다. **이 대비를 차트로 넣어라.**

**의석 기댓값은 어림값이다.** 양 끝 구간(`Below 190` 21.6%, `≤47` 22.6%)은 상·하한이 열려 있어 중앙값을 가정해야 한다. 그래서 **막대 분포가 주인공이고 기댓값은 각주**다. 스키마의 `expectation_is_approximate`가 이 사실을 문서에 실어 나른다.

## 6. 여론조사 채널

마진(D% − R%)을 확률로 바꾼다:

```
P(민주 승리) = Φ(마진 / σ),   σ = 6.0
```

σ=6은 "최종 마진의 오차 표준편차"로, 5%p 앞선 후보가 확률 약 80%가 되도록 하는 값이다.

**여론조사마다 무게가 다르다:**

| 가중치 | 언제 | 왜 |
|---|---|---|
| 1.00 | 양당 후보가 확정되고 이름으로 물은 조사 | 실제 대결을 잰 것 |
| 0.60 | "민주당 후보 vs 공화당 후보" 일반 대결 | 후보 개인의 자질은 못 재도, 우리가 알고 싶은 D-vs-R 사건 자체는 정확히 가리킴 |
| 0.35 | 예비선거 전 가상 대결 | 그 후보가 본선에 못 나올 수 있음 |
| 0.15 | 사퇴·낙선 후보가 포함된 조사 | 그래도 그 주의 정당 지형은 알려줌 |

여기에 **최신성(반감기 14일)**과 **표본 크기(√n)**를 곱한다.

**한 조사 = 한 줄 규칙:** 한 여론조사가 여러 대결을 물으면 시트에 여러 줄로 들어온다. 그대로 쓰면 그 기관의 무게가 대결 수만큼 뻥튀기된다 (Marquette 7월 위스콘신 조사 하나가 5줄이었다). 가장 신뢰도 높은 등급의 대결만 남겨 평균 내어 **한 줄로 접는다.**

**예비선거 여론조사는 제외한다.** 본선 사건을 재는 게 아니다.

## 7. 기초 지표 4개 모델

15개 변수를 사람이 읽을 수 있는 질문 4개로 묶었다 (`models.py`).

| 모델 | 질문 | 변수 |
|---|---|---|
| 경제 | 주 경제가 좋아지나, 나빠지나? | 실업수당 청구·경기동행지수·실업률 (FRED) |
| 정치자금 | 어느 쪽이 지역 기부자를 더 움직이나? | FEC 5종 (주내 비중·소액기부 건수·고유 기부자·재기부율·소진율) |
| 풀뿌리 | 실제 투표 행동은 어느 쪽인가? | 예비선거 투표율 격차·정당 등록 순변동 |
| 관심도 | 이 선거가 평소보다 주목받나? | 위키 조회·편집 (+ 가중치 0인 gdelt/reddit/youtube) |

### 확률이 아니라 방향과 세기다 — 이 문단을 크게 실어라

z-score는 "이 지표가 자기 평소 수준 대비 1.4 표준편차"라고 말할 뿐이다. 이를 **"민주당 57% 승리"로 바꾸려면 지표와 결과를 잇는 적합된 대응 관계가 필요한데 그게 없다** (과거 표본 47건, 상당수가 근사값). 그래서 v3는 이 값들로 확률을 만들지 않는다.

상세보기의 `±X%p`는 같은 z를 익숙한 단위로 환산한 **참고 눈금**이며 (`config.PP_PER_SIGMA = 5.0`), 그 값은 옛 λ=0.10·z_norm=2.0에서 그대로 이어받아 추적 가능하게 두었다.

### 설계 원칙: 설문이 아니라 행동을 잰다

여론조사는 이미 별도 채널에 있다. 기초 지표가 또 설문을 쓰면 같은 증거를 두 번 세는 셈이다. 그래서 **사람이 실제로 한 행동**만 본다 — 돈을 냈는가, 투표하러 갔는가, 실업수당을 신청했는가.

- **예비선거 투표율**: 실제로 투표소에 간 사람 수. "열정적입니까?"라는 설문 응답보다 훨씬 강한 증거이고, 정치학 문헌의 표준 열정격차 측정법이다.
- **주내 기부 비중**: 전국 모금 조직을 가진 후보와 **자기 지역구 유권자가 실제로 움직이는** 후보를 구분한다. 상원 후보는 자금 대부분을 주 밖에서 걷을 수 있어 총액만 보면 지역 열기를 잘못 읽는다.
- **경제**: 나머지가 전부 "열정" 계열이라 서로 겹친다. 경제는 그것들과 거의 무관해서 **독립적인 정보**를 더한다. 회고적 투표는 선거 예측 문헌에서 가장 확립된 발견이기도 하다.

### 경제 변수의 귀속 규칙

경제가 좋아지면 누가 덕을 보는가 — 직위마다 다르다:

- **주지사** → 그 주지사직을 쥔 정당 (유권자는 주 경제를 주 행정부에 묻는다)
- **상·하원** → **대통령 정당** (중간선거는 국정 심판이고, 초선 하원의원이 주 실업률 책임을 지지는 않는다)

그래서 **같은 주 같은 경제인데 부호가 반대로 나올 수 있다.**

### 관심도는 방향을 갖지 않는다

조회수는 **관심이지 지지가 아니다.** 스캔들이 터져도, 지지가 급등해도 똑같이 올라간다. 그래서 정당이 아니라 **수준(높음/보통/낮음)만** 보고하고 `shift_pp`도 없다. **스키마가 이걸 강제한다.** (구글 트렌드를 뺀 것도 같은 이유다.)

### z-score와 결측 처리

모든 변수는 **자기 자신의 과거와 비교한 z-score**로 들어간다.

```
z = (이번 관측값 − 자기 과거 평균) / 자기 과거 표준편차,   ±3σ로 클립
```

"오하이오 민주당이 기부를 10만 달러 받았다"는 그 자체로 의미가 없다. **"평소보다 2σ 많이 받았다"**가 신호다.

모델 안에서 가중치는 **가용한 변수 위에서만 재정규화된다.** 5개 중 1개만 살아 있으면 그 1개의 값을 보고하지, 5분의 1로 눌린 값을 보고하지 않는다. 대신 `n_available/n_total`을 같이 실어서 읽는 사람이 스스로 할인하게 한다. (v2의 `√(n/N)` 보정은 하나로 합친 신호를 레이스 간 비교 가능하게 만들려던 장치였고, **합치지 않는 지금은 보정할 대상이 없다.**)

**결측은 세 종류로 구분된다 — 서로 다른 사실이고 스키마 수준에서 갈린다:**

| 상태 | 뜻 | 예 |
|---|---|---|
| `structural` | 제도적으로 불가능 | 주지사 선거는 FEC 관할 밖 |
| `pending` | 아직 시점이 안 됨 | 예비선거 전 |
| `missing` | 이번에 실패 | API 오류 |

## 8. 결과

§A 출력으로 채운다. 서술은 이 틀을 따라라:

- **세 채널이 가장 크게 갈리는 레이스 상위 몇 개**를 골라 시장 %·여론조사 %·기초 지표 방향을 나란히 놓아라. 어느 쪽이 맞다고 판정하지 마라. (v2 실측 예: 텍사스 상원 시장 49.0% vs 여론조사 51.1% — 부호 자체가 갈렸다.)
- **`trustworthy = false`인 레이스**는 숫자 대신 경고로 표시하고, 왜 그런지(무소속 질량) 설명하라.
- **여론조사·시장 칸이 빈 하원 안전 의석**이 상단에 있으면 §3의 이유를 붙여라. 고장이 아니다.
- **`movers`(어제 대비 변화)**가 비어 있으면 "첫 v3 런이라 비교 대상이 없다"고 쓰고 넘어가라. **억지로 v2 스냅샷과 비교하면 가짜 변동이 생긴다.**

## 9. 한계 (숨기지 말고 정면에 배치)

**① 기초 지표의 눈금은 검증되지 않았다.** 가중치와 `PP_PER_SIGMA = 5.0`은 과거 데이터로 적합한 값이 아니라 수기 설정값이다. 쓸 수 있는 과거 표본이 47건뿐이고 그중 상당수가 근사 재구성값이라 적합 자체가 의미를 갖지 못한다. **v3가 이 값들로 확률을 만들지 않는 이유가 정확히 이것이다.** `config.CALIBRATION_NOTE`와 `data/historical/MANIFEST.md`에 기록돼 있다.

**② 옛 백테스트는 변수 품질이 아니라 사후 정보량을 재고 있었다.** 근사 재구성 컬럼은 결과와 **+0.49~+0.72**로 상관됐는데, 경합지만 모은 표본에서 단일 변수가 그런 상관을 갖는 일은 현실에 없다 — 결과를 알고 쓴 숫자의 특징이다. 반면 FRED에서 실제로 계산한 경제 컬럼은 **+0.12 / +0.05 / −0.01**로, 이게 진짜 표본 외 데이터의 모습이다. **이 대비를 차트로 넣어라.** v3에서 `backtest.py`를 삭제한 근거다.

**③ 예비선거 투표율의 기준선이 횡단면이다.** 주별 관측치가 하나뿐이라 그 주의 과거가 아니라 **같은 사이클의 다른 19개 주와** 비교한다. "이 주치고 이례적인가"가 아니라 "이번 사이클 다른 주 대비 이례적인가"를 재는 것이라, 모든 주가 같은 방향으로 움직인 사이클에서는 신호가 약해진다.

**④ 하원 지역구는 주(州) 경제 수치를 쓴다.** 월간 지역구 단위 경제 시계열이 존재하지 않는다. 지역구는 주가 아니므로 **이 변수는 보드가 가장 조밀한 하원에서 가장 약하다.**

**⑤ 하원 보드는 접전 순이 아니라 거래액 순이다.** §3 참조. 사용자가 수치를 보고 내린 결정이지만, 한계인 것은 맞다.

**⑥ 의석 기댓값은 어림값이다.** §5 참조. 양 끝 구간이 열려 있다.

**⑦ 표본이 작다.** 과거 데이터 47건. 사이클별 홀드아웃 정밀도가 0.00 / 0.50 / n/a / 1.00 / 0.00으로 널뛴다. **이건 신호가 아니라 잡음이다.**

## 10. 작동하지 않는 것 (정직한 목록 — 이 섹션도 반드시 넣어라)

| 변수 | 상태 | 이유 |
|---|---|---|
| GDELT (미디어) | 가중치 0 | API 상시 429. 응답 본문이 스스로 "벌크 파일로 옮기라"고 안내 — 접근 방식 문제지 장애가 아니다. 재구축은 8주치 5,376개 파일이라 며칠 작업 |
| 레딧 | 가중치 0 | OAuth 필수, 무인증 경로 403 |
| 유튜브 | 가중치 0 | API 키 필요 |
| 정당 등록 순변동 | 가중치 0 | 값이 생성되는 레이스 0곳. 44개 주는 제도 자체가 없고, 있는 6개 주는 데이터 미입력 |

무인증 대안도 실측 확인했으나 전부 막혀 있었다: **Bluesky 공개 API 403, Reddit 무인증 JSON 403 ("Blocked")**.

**죽은 변수를 0으로 정리하고 나머지에 비례 재배분한 이유:** 어떤 변수가 더 낫다는 **새로운 주장을 하지 않기 위해서**다.

## 11. 후속 작업

| 시점 | 할 일 |
|---|---|
| 8월 11일 이후 | 위스콘신 예비선거 투표율 한 줄 추가 |
| 매일 | 두 번째 v3 런부터 `movers`(전일 대비 변화)가 채워진다 |
| 여유 생기면 | GDELT를 벌크 파일로 재구축하면 미디어 변수가 살아난다 (며칠짜리) |

## 12. 결론 4문장 (이 톤 그대로 마무리)

- **만든 것**: 베팅 시장·여론조사·기초 지표 세 채널을 **합치지 않고 나란히** 보여주는 모니터. 보드는 시장 거래액이 정하고, 의석·다수당 확률은 계산하지 않고 챔버 시장에서 읽는다.
- **지금 말하는 것**: §A 출력에서 세 채널이 가장 크게 갈린 레이스를 지목하라. **어느 쪽이 맞다고 말하지 마라.**
- **믿어도 되는 것**: 데이터 수집과 각 채널의 계산 과정. 양쪽 다리 정규화, 정당별 합산, 무소속 질량 보고, 없는 데이터를 없다고 표시하는 것, 결측 3종 구분. 예비선거 투표율은 독립 소스(아이오와 주정부 인증 PDF)로 교차검증했다.
- **믿으면 안 되는 것**: 기초 지표의 `%p` 눈금을 확률처럼 읽는 것. 하원 상단이 접전 순이라고 가정하는 것. 의석 기댓값의 소수점. 이 도구의 출력은 **"확률이 얼마다"가 아니라 "여기를 들여다보라"**로 읽어야 한다.

## 13. 디자인 지시

- **톤**: 이코노미스트 / FT 데이터 리포트. 차분하고 활자 중심. 이모지·그라데이션 남발 금지.
- **구조**: 상단에 §A에서 가져온 핵심 수치 KPI 행. 그다음 **"이건 예측 모델이 아니다 — 세 채널을 합치지 않는다"** 경고 배너. 그 아래 본문.
- **정당 색**: 민주=블루 계열, 공화=레드 계열. 채도를 낮춰 리포트 톤을 유지하고, **색만으로 정보를 전달하지 마라**(부호·라벨 병기).
- **한계 섹션은 접지 마라.** 아코디언에 숨기지 말고 본문에 펼쳐 둬라. 이 프로젝트의 신뢰도는 한계를 드러낸 데서 나온다.
- **차트 최소 4개**: ① 세 채널이 갈리는 레이스 슬로프/덤벨 ② 챔버별 의석 분포 막대(§5) ③ 다수당 시장 vs 의석 분포 교차검증 대비(45.5% vs 46.2%) ④ 근사 컬럼 상관 +0.49~0.72 vs 실측 경제 +0.12/+0.05/−0.01 (§9-②).
- **수식은 등폭 폰트 블록**으로 눈에 띄게.

## 14. 절대 규칙

- **§A 출력에 없는 숫자를 지어내지 마라.** 데이터가 부족하면 그 자리를 비우고 "미측정"이라고 써라.
- **`p_consensus` / `p_alpha` / `Δ` / `FLAG` / `λ` 를 쓰지 마라.** v3에 존재하지 않는다. 옛 보고서를 참고했다면 그 대목은 전부 폐기된 것이다.
- **세 채널을 평균 내지 마라.** 보고서 안에서조차 하지 마라. 그게 이 버전의 요점이다.
- 예측 정확도·적중률 같은 건 **존재하지 않는다.** 선거가 아직 안 치러졌다.
- "이 모델이 시장보다 낫다"는 취지의 문장을 쓰지 마라. 도구의 목적은 **차이의 표시**지 우열 판정이 아니다.
- 한계 7개와 "작동하지 않는 것" 표는 **하나도 빼지 마라.**
