# 내일 할 일 (2026-08-09)

2026-08-08 작성. **프로그램이 v3로 전면 재설계됐습니다.** 이전 문서의
FLAG·컨센서스·Δ 이야기는 전부 없어졌습니다. 이 문서만 보시면 됩니다.

---

## ⚠️ 제일 먼저: 전체 런 한 번

**어제 전체 런이 끝나지 못했습니다** (65개 중 47개까지 하고 터미널을 닫음).
그래서 `forecast.json`은 아직 **옛 v2 파일(8/2자)** 입니다. 대시보드를 지금
열면 "v2.2.0 형식입니다"라고 뜹니다. 정상이고, 아래 한 줄로 해결됩니다.

```bash
cd ~/Desktop/SCBK_Intern_Project/2026_election_prediction
python3 -m election2026 run --skip gdelt,reddit,youtube
```

**`--skip`을 꼭 붙이세요.** 안 붙이면 GDELT가 차단당한 채 재시도를 반복해
**1시간 40분쯤 멈춰 있습니다.** 세 변수 모두 가중치 0이라 결과는 같습니다.

**시간이 걸립니다.** 어제 FEC 데이터를 39MB 받아놨고 그중 오래 걸리는
부분(레이스별 신고 시점 탐색)은 1주일 캐시라, 어제보다는 빠릅니다. 그래도
**20~40분** 잡으세요. 화면이 조용해도 정상입니다.

끝나면 이런 요약이 나옵니다:
```
[pipeline] wrote .../forecast.json — 65 races
[pipeline]   상원: 민주당 46%, 예상 의석 D 50.1 / R 49.9 (20개 레이스 표시)
[pipeline]   하원: 민주당 87%, 예상 의석 D 231.7 / R 203.3 (25개 레이스 표시)
[pipeline]   주지사: 장악 시장 없음, 예상 의석 D 26.2 / R 23.8 (20개 표시)
```

## 그다음: 대시보드

```bash
python3 -m election2026.serve
```
→ 브라우저에서 **`http://localhost:8000/dashboard.html`**

⚠️ **파일을 더블클릭해서 열면 안 됩니다.** 빈 화면이 나옵니다. 반드시 위
명령을 먼저 실행하고 주소로 들어가세요. (주소가 바뀌었습니다 —
`divergence-monitor.html`이 아니라 `dashboard.html`입니다.)

---

## 화면 읽는 법

### 맨 위 — 전체 판세
`Balance of Power` 시장. 미드텀 전체에서 **거래액이 가장 큰 시장**($9.5M)이고
상원·하원 결과를 한꺼번에 가격 매깁니다. 어제 기준 민주 석권 45.4% /
상원공화+하원민주 40.4% / 공화 석권 12.5%.

### 의회별 전망
상원·하원·주지사 각각:
- 큰 숫자 = **다수당이 될 확률** (시장에서 그대로 읽은 값, 우리가 계산한 게 아님)
- 막대 = 의석수 분포. 파란 구간 = 민주당 다수, 빨간 구간 = 공화당 다수
- **의석수 숫자는 어림값입니다.** 양 끝 구간(`Below 190` 같은)이 열려 있어
  중앙값을 가정해야 하기 때문입니다. 막대 모양이 진짜 정보입니다.

### 표 — 개별 선거
왼쪽부터: 순위 · 선거구 · **베팅 시장 %** · **여론조사 %** · **기초 지표** · 거래액.

**셋이 서로 다른 게 정상이고, 그게 핵심입니다.** 어제 실측 예: 텍사스 상원은
시장 49.0%(공화 우세)인데 여론조사 51.1%(민주 우세). 이전 버전은 이 둘을
평균 내 하나로 만들었는데, 그러면 이 불일치가 사라집니다.

**행을 클릭하면** 기초 지표 4개가 펼쳐집니다.

| 모델 | 무엇을 보나 |
|---|---|
| 경제 | 그 주 경제가 좋아지나 나빠지나 (실업수당·실업률·경기지수) |
| 정치자금 | 어느 쪽 후보가 지역 기부자를 더 많이 움직였나 (FEC) |
| 풀뿌리 | 예비선거에서 실제로 누가 더 표를 받았나 |
| 관심도 | 평소보다 주목받나 (**방향 없음** — 조회수는 지지와 반감을 구분 못 함) |

**이건 확률이 아니라 방향(▲민주 / ▼공화)과 세기입니다.** "경제가 나빠지고
있다"를 "민주당 57% 승리"로 바꾸려면 지표와 결과를 잇는 검증된 대응 관계가
필요한데 과거 자료가 47건뿐이라 그 환산을 하지 않습니다. 상세보기의 `%p`는
참고 눈금입니다.

---

## 알아두실 것 세 가지

### 1. 하원은 거래액 순 ≠ 접전 순
폴리마켓은 하원 **443개 지역구 전부**를 상장합니다. 거래액은 "얼마나 오래
상장돼 있었나"에 가까워서 결과가 뻔한 안전 의석(CA-28, FL-01 등)에도 돈이
쌓입니다. **상단에 접전이 아닌 곳이 올라올 수 있고, 그 행은 여론조사·경제
칸이 비어 보입니다. 고장이 아닙니다.** 대시보드가 하원 표 위에 이 경고를
직접 띄웁니다.

기존에 손으로 고르셨던 경합 18곳은 전부 하원 거래액 중앙값 아래였습니다
(최고 wa-03 $27K, 최저 pa-08 $1.2K, ca-22는 시장 자체가 없음). 경합지만
보시려면 `--races`로 좁히세요.

### 2. 첫 런에는 "변화" 칸이 비어 있습니다
어제와 비교해야 변화가 나오는데 기존 스냅샷은 옛 형식이라 비교 대상이 안
됩니다(억지로 비교하면 가짜 변동이 생깁니다). **모레(=v3로 두 번째)
돌리시면** 화살표와 "어제보다 많이 움직인 시장"이 채워집니다.

### 3. 네브래스카·몬태나에 노란 표시
무소속 후보에게 걸린 돈이 각각 26%·17%라 민주 대 공화 두 값으로 환산하면
실제로 거래되는 사건과 달라집니다. **숫자 대신 경고를 띄웁니다.** 예전에
손으로 제외했던 문제인데 이제 규칙이 알아서 잡아냅니다.

---

## 여론조사 새로 넣을 때

```bash
python3 -m election2026 import-polls 2026_senate_poll.xlsx 2026_House_poll.xlsx Gubernatorial_Polls_2026.xlsx
```
**세 파일을 항상 같이** 넘기세요. 하나만 넘기면 나머지 폴이 지워집니다.

---

## 확인해 주셨으면 하는 것

1. **`election2026/data/reference/candidate_parties.json`의 알래스카 후보
   정당 매핑.** 제가 채운 것이라 검증이 필요합니다. 알래스카는 정글
   프라이머리라 폴리마켓이 정당 라벨 없이 후보 22명을 나열해서, 이 파일이
   없으면 알래스카 상원·주지사가 통째로 빈칸이 됩니다.
   틀리거나 빠져도 **숫자가 조용히 틀리지는 않고 표시가 멈춥니다.**

2. **하원 볼륨 순 정렬이 실제 화면에서 쓸 만한지.** 어제 결정하신 대로
   넣었지만 실물로 보시면 판단이 달라질 수 있습니다. 바꾸고 싶으시면
   `election2026/config.py`의 `BOARD` 한 곳만 고치면 됩니다.

---

## 문제가 생기면

| 증상 | 조치 |
|---|---|
| "v2.2.0 형식입니다" 경고 | 아직 v3 `run`을 안 돌린 것. 맨 위 명령 실행 |
| 대시보드가 빈 화면 | 파일을 직접 열었을 가능성. `serve` 먼저 |
| 1시간 넘게 안 끝남 | `--skip gdelt,reddit,youtube` 빠뜨렸을 가능성. Ctrl-C 후 다시 |
| 어떤 선거구 지표가 전부 "—" | 정상일 수 있음. 시장은 거래되는데 우리 참조 데이터에 없는 선거구면 지표를 만들 수 없습니다 |
| 런이 중간에 끊김 | 그냥 다시 돌리면 됩니다. 받아둔 캐시는 남아 있어 이어서 진행됩니다 |

검증:
```bash
python3 -m election2026 validate                # 스키마 확인
python3 -m pytest election2026/tests -q         # 154개 통과해야 정상
```

---

## 어제까지 끝난 것

- `board.py` / `chambers.py` / `models.py` 신설, `divergence.py`·`backtest.py`·
  `comment.py`·`mockdata.py`·`track_a/consensus.py` 등 블렌딩 계층 삭제
- 스키마 v3.0.0 + 교차 검증 규칙, 테스트 **154개 통과**
- `dashboard.html` 새로 작성 (14MB 번들 폐기). 실제 데이터로 브라우저에서
  라이트·다크 모두 확인 완료
- 베팅 확률 버그 3개 수정 (양쪽 다리 정규화 / 정당별 합산 / 다리 없음≠0%)
- `README.md` 갱신

## 아직 안 한 것 / 남은 일

1. **전체 라이브 런** ← 내일 제일 먼저 (위 참조)
2. **WI 예선(8/11) 이후** 위스콘신 예비선거 투표율 한 줄 추가
3. 안 쓰는 파일 정리 — 원하시면 지워도 됩니다 (**총 ~28MB**):
   - `divergence-monitor.html`, `divergence-monitor.html.bak` (구 대시보드)
   - `2026 US Election Forecast.html` (더 옛날 프런트)
   - `election2026/data/mock/`, `election2026/data/historical/`
     (mock 실행·백테스트용이었는데 둘 다 삭제됨)
4. 옛 문서 3개가 아직 v2를 설명합니다 — 필요하시면 갱신하겠습니다:
   `INVENTORY.md`, `2026_중간선거_다이버전스_모니터_정리.md`,
   `보고서_생성_프롬프트.md`
5. (선택) GDELT를 벌크 파일로 재구축하면 미디어 변수를 살릴 수 있습니다 —
   며칠짜리 작업
